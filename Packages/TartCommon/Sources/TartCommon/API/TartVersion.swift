public enum TartVersion {
    /// Human-readable version reported by the management API `/health` endpoints. Bump this
    /// alongside the release tag / Homebrew formula versions.
    public static let current = "0.24.0"
}
